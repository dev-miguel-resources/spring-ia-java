spring:
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  ai:
    openai:
      api-key: XXXXX # Aquí deben poner la suya
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
            
          